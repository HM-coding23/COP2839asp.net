# Ch06Ex1RoutingPractice

This project practices default routing and attribute routing in ASP.NET Core MVC. I added the Countdown action to HomeController and updated it to use three parameters named start, end, and message. The custom route matches those parameter names, so the URL can count down from a starting number to an ending number and then show a message at the end.
