# Ch05Ex1MovieList

This project is the Movie List app for week 3. I fixed the bug on the home page so the genre column shows the genre name instead of the whole object. I also updated the Movie model so the Name field is required and starts as an empty string. That lets the app show a validation message instead of crashing when a movie name is left blank.
