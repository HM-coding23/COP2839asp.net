# Ch11Ex1 TempManager

This project adds validation to the TempManager app.

I added required and range validation to the Temp model, added model and property level messages in the Add view, added client side validation scripts, and added remote plus server side checks for duplicate dates.
