# Ch10Ex1 ToDoList

This project updates the ToDo List app so the views use a view model instead of binding directly to the entity model and ViewBag values.

I changed the Home controller and both Home views to use ToDoViewModel. This keeps the data for filters, drop down lists, tasks, and the current task together in one place.
